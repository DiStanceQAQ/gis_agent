# GIS Agent 后端开发任务清单（Manual Approval + ReAct FC）

更新时间：2026-04-05

这份清单仅保留后端任务，目标是实现你定义的 Demo 流程：  
**用户先审批（可改、可拒绝） + 每步 ReAct + Function Calling 执行 GIS 工具**。

状态约定：

- `[x]` 已完成
- `[~]` 进行中
- `[ ]` 待完成

已完成代码提交：

- `cd1a7c0`：BE-30 全量 task 绑定（parse/plan/recommend/react_step 审计闭环 + 失败日志补齐）。
- `3a9f51f`：审批拒绝链路 + ReAct/Function Calling 事件链 + 非空计划与 schema 对齐。
- `66039ab`：Step ReAct 决策 schema + runtime guardrails（单步预算配置）。
- `df59fcd`：拒绝后阻断批准回归 + 非法 function call 失败语义测试。
- `a515773`：流程级回归补齐（编辑计划后批准执行、拒绝执行取消、流程内 ReAct/FC 事件断言）。
- `d4e5f6b`：任务清单同步更新（记录流程级回归提交）。
- `6368ac0`：BE-31 审计可定位性测试补齐（task_id 审计视图 + LLM call log 字段完整性）。
- `639ce34`：BE-22 去占位成功产物（移除 pipeline 末尾占位兜底 + 补失败回归测试）。
- `e4180b0`：消息响应 schema 与 intent-routing 配置开关/阈值落地。
- `1697567`：message mode/intent 字段约束收紧 + schema 回归测试。
- `e5dcb5e`：新增 intent 分类与 chat 回复服务及 prompt。
- `80e6861`：intent/chat LLM 返回 payload 校验与 fallback 加固。
- `427a10c`：确认类消息在 intent payload 非法时回退到 task 确认。
- `f77c0b3`：非 dict intent payload 安全回退。
- `b5f6eef`：`/messages` 按意图分流 chat/task，并新增路由回归测试。
- `49abf08`：确认上下文解析加固，避免误用历史消息。
- `0e4dcfc`：确认执行绑定最近有效任务请求，稳定上传文件回放顺序。
- `eefed78`：assistant chat 消息历史回放断言补齐。
- `f87884d`：历史回放断言调整为行为导向，降低实现耦合。
- `4df414b`：用 chat LLM stub 做黑盒式历史回放验证。
- `b8b5580`：parser/planner 支持 clip 请求并降低模板耦合（含 schema 与回归）。
- `a487196`：`operation_plan` 改为基于 `operation_registry` 自动生成 DAG（含 builder 与全量回归）。

---

## 0. 当前目标定义（Backend Only）

1. 任务默认进入审批态，批准前不执行。  
2. 用户可拒绝执行，任务进入 `cancelled`。  
3. 每个计划步骤都通过 ReAct + Function Calling 执行。  
4. `OperationPlan` 非空且合法，禁止空计划成功。  
5. 全链路审计可按 `task_id` 查询。

---

## 1. 审批必选与拒绝路径（优先级 P0）

- [x] `BE-00` 任务创建默认进入 `awaiting_approval`
  文件：`packages/domain/services/orchestrator.py`、`packages/schemas/message.py`、`packages/schemas/task.py`
  完成标准：
  1. 创建任务后 `need_approval=true`。  
  2. 未批准时不会入队。  
  3. 任务详情可返回可编辑计划草稿。

- [x] `BE-01` 拒绝执行接口与状态流
  文件：`apps/api/routers/tasks.py`、`packages/domain/services/orchestrator.py`、`packages/domain/services/task_state.py`
  完成标准：
  1. 新增 `POST /tasks/{task_id}/reject`。  
  2. 拒绝后状态 `cancelled`。  
  3. 被拒绝任务不可执行、不可再次批准（除非显式重开策略）。

- [x] `BE-02` 审批/拒绝事件闭环
  文件：`packages/domain/services/task_events.py`、`packages/domain/services/orchestrator.py`
  完成标准：
  1. 审批写 `task_plan_approved`。  
  2. 拒绝写 `task_plan_rejected` + `task_cancelled`。  
  3. 事件 detail 包含 `reason`、`approved_version` 等关键字段。

---

## 2. Step ReAct + Function Calling（优先级 P0）

- [x] `BE-10` 定义 Step ReAct 决策 schema
  文件：`packages/schemas/agent.py`、`packages/domain/prompts/*`
  完成标准：
  1. 结构化字段至少包含 `decision`、`function_name`、`arguments`、`reasoning_summary`。  
  2. 支持 `continue/skip/fail` 决策。  
  3. schema 校验失败有明确错误码。

- [x] `BE-11` 实现每步 ReAct loop
  文件：`packages/domain/services/graph/nodes.py`、`packages/domain/services/graph/runtime_helpers.py`
  完成标准：
  1. 每个步骤执行 `reason -> decide -> call -> observe` 回合。  
  2. 回合结束条件明确（成功/失败/超预算）。  
  3. 结果回写到 step detail 与事件流。

- [x] `BE-12` Function Calling 白名单执行器
  文件：`packages/domain/services/tool_registry.py`、`packages/domain/services/graph/runtime_helpers.py`
  完成标准：
  1. 仅允许白名单 function 调用。  
  2. 非法 function name 或参数直接失败并写错误码。  
  3. 工具调用输入输出都结构化记录。

- [x] `BE-13` ReAct 边界保护
  文件：`packages/domain/config.py`、`packages/domain/services/graph/runtime_helpers.py`
  完成标准：
  1. 新增单步回合数与超时配置。  
  2. 超预算时任务失败语义清晰。  
  3. 不影响现有全局 steps/tool_calls/timeout 保护。

---

## 3. 计划真实性与白名单收口（优先级 P0）

- [x] `BE-20` 强制非空 `operation_plan.nodes`
  文件：`packages/domain/services/plan_guard.py`、`packages/domain/services/orchestrator.py`
  完成标准：
  1. 创建、编辑、审批三个入口都校验非空节点。  
  2. 空计划返回结构化错误码。  
  3. 空计划不可进入执行态。

- [x] `BE-21` Operation schema 与 registry 对齐
  文件：`packages/schemas/operation_plan.py`、`packages/domain/services/operation_registry.py`
  完成标准：
  1. `AllowedOperationName` 覆盖已实现操作。  
  2. 不出现“运行时支持但 API 不可表达”。  
  3. 对齐后相关 schema 测试通过。

- [x] `BE-22` 移除占位成功路径
  文件：`packages/domain/services/processing_pipeline.py`、`packages/domain/services/graph/runtime_helpers.py`
  完成标准：
  1. 空计划时直接失败。  
  2. 不生成占位成功产物。  
  3. 增加回归用例防止回退。  
  说明：移除 pipeline 末尾占位 `tif/png` 兜底；`artifact.export` 在缺失真实栅格输入时直接失败；新增向量-only 与伪 geotiff 导出失败回归用例。

---

## 4. LLM 审计闭环（优先级 P0）

- [x] `BE-30` parse/plan/recommend/react_step 全量 task 绑定
  文件：`packages/domain/services/orchestrator.py`、`packages/domain/services/parser.py`、`packages/domain/services/planner.py`、`packages/domain/services/llm_client.py`
  完成标准：
  1. 四阶段 LLM 日志均可按 `task_id` 查询。  
  2. 主链路避免 `task_id=None`。  
  3. 失败和成功日志都落库。  
  说明：主链路创建任务时先落 provisional task 并绑定 parse 日志；plan/recommend 维持 task 绑定；新增 step 级 `react_step` 审计日志；API key 缺失等失败路径也会写入 LLM 日志。

- [x] `BE-31` 审计查询与可定位性测试
  文件：`tests/test_llm_client.py`、`tests/test_llm_call_logs_model.py`、`tests/test_task_samples.py`
  完成标准：
  1. 覆盖审批、拒绝、ReAct、Function Calling、失败路径。  
  2. 覆盖 `failed_step/error_code/reject_reason` 查询。  
  3. 审计字段完整性断言稳定通过。  
  说明：新增按 `task_id` 的审计视图断言（审批/拒绝/失败三条路径），并补充 `llm_call_logs` 持久化与字段完整性验证。

---

## 5. 对话意图路由与执行确认（优先级 P0）

- [x] `BE-50` 聊天/执行意图路由
  文件：`apps/api/routers/messages.py`、`packages/domain/services/orchestrator.py`、`packages/domain/services/intent.py`、`packages/domain/services/chat.py`、`packages/domain/prompts/intent.md`、`packages/domain/prompts/chat.md`
  完成标准：
  1. `/messages` 可区分普通聊天与任务请求。
  2. 普通聊天直接返回 assistant 文本，不创建任务。
  3. 任务请求先返回执行确认提示，确认后再进入任务创建/审批链路。
  说明：新增 LLM intent classifier 与 chat reply 服务；支持 `chat/task/ambiguous` 三类意图、置信度阈值与本地 fallback；`/messages` 响应返回 `mode/intent/intent_confidence/awaiting_task_confirmation`。

- [x] `BE-51` 执行确认门槛
  文件：`packages/domain/services/orchestrator.py`、`packages/domain/services/intent.py`、`tests/test_message_intent_routing.py`、`tests/test_plan_approval_flow.py`
  完成标准：
  1. 高置信 task 意图先要求用户显式确认。
  2. “好的/继续/ok”等确认消息仅在最近一条 assistant 确认提示后生效。
  3. 找不到可执行上下文时降级为 chat 提醒补充任务信息，不误触发执行。
  说明：确认解析绑定最近一条确认 prompt 与其前序用户请求；确认时可回放最近上传文件；对非法 intent payload、非 dict payload、孤立确认消息均提供安全 fallback。

- [x] `BE-52` assistant 消息落库与回放
  文件：`packages/domain/services/orchestrator.py`、`packages/domain/services/chat.py`、`packages/domain/models.py`、`tests/test_message_intent_routing.py`
  完成标准：
  1. assistant 回复与确认提示持久化到 `messages`。
  2. 后续 chat 轮次构造 LLM prompt 时带入历史 user/assistant 消息。
  3. 历史回放按时间和 ID 稳定排序，并有黑盒回归测试覆盖。
  说明：chat/确认分支统一落 assistant 消息；history 查询排除当前 user message 后按 `created_at,id` 倒序取窗再回正序；新增二轮对话黑盒测试验证 assistant 历史可回放。

- [x] `BE-53` 基于 `operation_registry` 自动生成 `operation_plan` DAG（替代固定默认模板）
  文件：`packages/domain/services/operation_plan_builder.py`、`packages/domain/services/orchestrator.py`、`packages/domain/services/graph/nodes.py`、`packages/domain/services/processing_pipeline.py`、`tests/test_operation_plan_builder.py`
  完成标准：
  1. 任务草案 `operation_plan` 由注册表驱动自动构建，不再固定 `raster.clip + artifact.export` 模板。
  2. 支持从 `analysis_type` 与 `operation_params.operations` 推导操作链，并自动补 `artifact.export` 与依赖关系。
  3. 保持现有 runtime 与回归稳定（全量测试通过）。
  说明：当前 runtime 仍是 raster-first 产物链路，`BUFFER` 默认映射为 raster-compatible 链；若要“任意操作自由组合”需继续推进 runtime 对非 raster 终态的统一产物契约。

- [x] `BE-54` planner 直出操作级 DAG + 非 raster 终态契约
  文件：`packages/schemas/agent.py`、`packages/domain/services/planner.py`、`packages/domain/prompts/planner.md`、`packages/schemas/task.py`、`packages/domain/services/orchestrator.py`、`packages/domain/services/graph/nodes.py`、`packages/domain/services/processing_pipeline.py`、`packages/domain/services/graph/runtime_helpers.py`
  完成标准：
  1. Planner 支持输出 `operation_plan_nodes`，并通过后端校验后落入任务计划。
  2. 任务建草案时优先采用 planner 提供的操作 DAG（非法时回退 registry builder）。
  3. `run_processing_pipeline` 与 `generate_outputs` 支持纯矢量/表格等非 raster 终态，不再强制 tif 才能成功。
  说明：已补齐单测与回归（含 `tests/test_planner.py`、`tests/test_orchestrator.py`、`tests/test_processing_pipeline.py`，全量测试通过）。

---

## 6. 测试与验收（优先级 P0）

- [x] `BE-40` 回归样例补齐（流程级）
  文件：`tests/fixtures/tasks/*.json`、`tests/test_task_samples.py`
  完成标准：
  1. 覆盖“直接批准执行”。  
 2. 覆盖“修改计划后批准执行”。  
 3. 覆盖“拒绝执行->cancelled”。  
 4. 覆盖“每步 ReAct + Function Calling”。  
 说明：已补充 `approval_edit_plan_then_execute` 与 `approval_reject_then_cancelled` 样例，并在流程断言中加入 ReAct/Function Calling 事件检查。

- [x] `BE-41` 关键单测补齐（能力级）
  文件：`tests/test_graph_runtime.py`、`tests/test_plan_approval_flow.py`、`tests/test_operation_plan_schemas.py`
  完成标准：
  1. 审批前不可执行。  
  2. 拒绝后不可执行。  
  3. 空计划必失败。  
  4. 非法 function call 必失败。

- [x] `BE-42` CI 绿线
  文件：`.github/workflows/ci.yml`
  完成标准：
  1. `compileall + ruff + pytest` 全通过。  
  2. 新增 ReAct/审批用例稳定无随机失败。
  说明：CI 工作流已包含 `python -m compileall apps packages tests`、`ruff check apps packages tests`、`pytest -q tests`；本地重复验收通过（当前基线 264/264）。

---

## 7. 建议执行顺序

1. 第 1 批：`BE-00 ~ BE-02`。  
2. 第 2 批：`BE-10 ~ BE-13`。  
3. 第 3 批：`BE-20 ~ BE-22`。  
4. 第 4 批：`BE-30 ~ BE-31`。  
5. 第 5 批：`BE-50 ~ BE-52`。
6. 第 6 批：`BE-40 ~ BE-42`。
